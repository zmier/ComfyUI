# ComfyUI 项目启动指南

本文档记录了如何设置和启动 ComfyUI 项目。

## 1. 创建虚拟环境

为了隔离项目依赖，我们首先创建一个 Python 虚拟环境。

```bash
python3 -m venv .venv
```

## 2. 使用 Makefile 管理项目

为了简化项目的管理（安装依赖、启动等），我们创建了一个 `Makefile`。

**Makefile 内容:**

```makefile
# Makefile for ComfyUI

# 定义变量
# VENV_DIR: 虚拟环境目录
# VENV_ACTIVATE: 激活虚拟环境的脚本
# PYTHON: 虚拟环境中的 Python 解释器
# PIP: 虚拟环境中的 pip
VENV_DIR = .venv
VENV_ACTIVATE = $(VENV_DIR)/bin/activate
PYTHON = $(VENV_DIR)/bin/python
PIP = $(VENV_DIR)/bin/pip

# 默认目标, 执行 make 时默认执行 help
.PHONY: all
all: help

# 显示帮助信息
.PHONY: help
help:
	@echo "Usage: make [target]"
	@echo "Targets:"
	@echo "  install    - Install dependencies"
	@echo "  run        - Run ComfyUI"
	@echo "  clean      - Remove virtual environment"

# 安装依赖
# 首先确保虚拟环境存在, 然后安装 requirements.txt 中的依赖
.PHONY: install
install: $(VENV_DIR)
	@echo "Installing dependencies..."
	@source $(VENV_ACTIVATE); $(PIP) install -r requirements.txt

# 运行 ComfyUI
# 首先确保虚拟环境存在, 然后运行 main.py
.PHONY: run
run: $(VENV_DIR)
	@echo "Starting ComfyUI..."
	@source $(VENV_ACTIVATE); $(PYTHON) main.py

# 创建虚拟环境
# 如果 $(VENV_DIR) 目录不存在, 则创建它
$(VENV_DIR):
	@echo "Creating virtual environment..."
	@python3 -m venv $(VENV_DIR)

# 清理虚拟环境
# 删除虚拟环境目录
.PHONY: clean
clean:
	@echo "Removing virtual environment..."
	@rm -rf $(VENV_DIR)
```

## 3. 安装依赖

创建好 `Makefile` 后，使用以下命令一键安装所有项目依赖。

```bash
make install
```

该命令会自动激活虚拟环境并从 `requirements.txt` 文件中安装所需的库。

## 4. 启动项目

安装完依赖后，使用以下命令来启动 ComfyUI 服务。

```bash
make run
```

服务启动后，您可以根据终端的提示信息访问 ComfyUI 的 Web 界面。
