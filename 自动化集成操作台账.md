# ComfyUI-Photoshop 插件自动化集成台账

本文档记录了构建自动化脚本以将 ComfyUI 集成到 Photoshop 的完整思考、行动和观察过程。

## 阶段一：分析与规划

### 思考

1.  **目标**: 自动化 `doc_comfyui-photoshop集成方法.md` 中描述的流程。
2.  **用户环境**: macOS (M4), Photoshop 2025, Homebrew 可用。
3.  **工具选择**: 用户建议 `Makefile` 或 `Python`。这是一个多步骤的过程，包含逻辑判断（如检查文件是否存在），因此一个 Python 脚本是核心，再用一个 Makefile 来调用它，会非常清晰。Makefile 作为入口，Python 脚本负责复杂的执行逻辑。
4.  **自动化范围分析**:
    *   **可完全自动化**:
        *   通过 Homebrew 安装 `zxp-installer`。
        *   使用 `git` 克隆或更新所需的 ComfyUI 插件 (custom nodes)。
        *   下载所有必需的模型文件 (Checkpoints, LoRAs, ControlNet, Upscaler)，但需要先检查文件是否已存在，避免重复下载。
        *   使用 `zxp-installer` 的命令行工具来安装 `.ccx` 插件文件。
    *   **不可/不应自动化**:
        *   Photoshop 和 Creative Cloud 的安装与登录。这需要用户手动完成，脚本应将其作为**前提条件**。
        *   ComfyUI 内部的语言设置。直接修改配置文件风险较高，可能会覆盖用户的其他设置。最好的方法是在脚本结束时**明确提示用户手动设置**。
        *   启动 ComfyUI 和 Photoshop，以及在 PS 中连接插件。这是用户的实际操作，不属于安装流程。
5.  **关键信息搜集**:
    *   我需要找到所有必需模型的直接下载链接。文档里没有提供，我需要通过网络搜索（比如在 Hugging Face 上）来找到它们。
    *   我需要确认 `zxp-installer` 的 Homebrew 包名和它的命令行用法。
6.  **最终方案**:
    *   创建一个名为 `setup_photoshop_integration.py` 的 Python 脚本。
    *   创建一个 `Makefile`，其中包含一个 `install-ps-plugin` 目标，该目标执行 Python 脚本。
    *   Python 脚本将执行所有自动化步骤，并在开始时检查前提条件，在结束时提供清晰的手动操作指引。
    *   我将边执行边更新这份台账。

## 阶段二：信息搜集

### 思考

我的首要任务是找到自动化脚本所必需的外部信息：模型的直接下载链接，以及 `zxp-installer` 的 Homebrew 包名和命令行用法。如果找不到直接链接，我会寻找包含这些信息的网页（如Hugging Face或Civitai）。

### 行动

我使用了 `google_web_search` 来查找这些信息。

### 观察

1.  **ZXP Installer**: 我发现了一个比 `zxp-installer` 更好的工具：Adobe 自己的 **Unified Plugin Installer Agent (UPIA)**。这是一个命令行工具，随 Creative Cloud 一起安装，非常适合自动化。在 macOS 上的路径是 `/Library/Application Support/Adobe/Adobe Desktop Common/RemoteComponents/UPI/UnifiedPluginInstallerAgent`。
2.  **Model URLs**: 我成功找到了所有必需模型的直接下载链接，包括来自 Hugging Face 和 Civitai 的稳定链接。

## 阶段三：编写 Python 自动化脚本

### 思考

基于收集到的信息，我开始编写 `setup_photoshop_integration.py` 脚本。脚本的逻辑结构如下：
1.  定义所有常量，包括路径、仓库地址和模型下载链接。
2.  检查先决条件，最重要的是检查 UPIA 工具是否存在。
3.  通过 `git` 克隆或更新所有必需的 ComfyUI 自定义节点。
4.  使用 `requests` 库下载所有模型，并添加 `tqdm` 进度条以优化用户体验。在下载前会检查文件是否存在。
5.  调用 UPIA 工具来安装 Photoshop 插件。
6.  在脚本末尾打印出清晰的、需要用户手动完成的后续步骤。

### 行动

我使用 `write_file` 工具创建了 `setup_photoshop_integration.py` 文件，并写入了完整的 Python 代码。

## 阶段四：创建 Makefile 入口

### 思考

为了给用户提供一个简单的命令入口，我决定在现有的 `Makefile` 中增加一个新目标 `install-ps-plugin`。这个目标会调用我们刚刚创建的 Python 脚本。

### 行动

我通过 `read_file` 读取了现有 `Makefile` 的内容，然后使用 `replace` 工具添加了新的 `install-ps-plugin` 目标，并更新了 `help` 信息。

## 阶段六：计划变更与最终方案

### 思考

用户反馈在他们的系统上找不到 Adobe 的 `UnifiedPluginInstallerAgent` 工具，原因是使用了非官方版本的 Photoshop，导致 Creative Cloud 安装不完整。最初的自动化方案因此受阻。

我需要立刻调整方案，切换到备用计划：使用第三方的 `ZXP Installer` 工具。这个新方案的优点是不再依赖任何 Adobe 的官方命令行工具，缺点是无法完全自动化插件安装的最后一步，需要用户手动拖拽一下文件。

我的新计划是：
1.  修改 `setup_photoshop_integration.py` 脚本。
2.  移除所有和 `UPIA` 相关的代码。
3.  增加通过 Homebrew 自动安装 `ZXP Installer` 的功能。
4.  脚本的核心任务（下载节点和模型）保持不变。
5.  在脚本的最后，提供清晰、简单的手动操作指南，指导用户如何使用 ZXP Installer 完成安装。

### 行动

我使用 `read_file` 和 `replace` 工具，重写了 `setup_photoshop_integration.py` 脚本，实现了上述新逻辑。我还更新了操作台账的这一部分，以反映最终的解决方案。

### 最终成果 (修订版)

*   **`setup_photoshop_integration.py`**: 一个更加健壮的 Python 脚本，它会自动安装 ZXP Installer，下载所有必需的文件，并为用户提供清晰的最终手动安装步骤。
*   **`Makefile`**: `install-ps-plugin` 命令现在将运行这个新脚本，整个流程对用户来说依然是无缝的。
*   **`自动化集成操作台账.md`**: 完整记录了从最初计划到最终方案的整个决策和开发过程，并补充了手动安装插件的成功经验。

## 阶段七：手动安装插件与自动化改进

### 思考

尽管 `setup_photoshop_integration.py` 脚本能够自动下载所有文件并提示使用 ZXP Installer，但由于用户使用的是“学习版”Photoshop，ZXP Installer 无法正常工作（报错：`Error:Extension Manager init failed,status -193! A required resource is missing.Please relaunch the Creative Cloud app and try again.`）。这表明 ZXP Installer 依赖于完整的 Adobe Creative Cloud 环境。

用户通过手动尝试，成功将插件安装到 Photoshop 中。这揭示了即使 ZXP Installer 失败，直接将插件文件放置到 Photoshop 期望的目录中仍然是可行的。

为了提高脚本的健壮性，特别是针对非官方 Photoshop 版本，我们需要改进 `setup_photoshop_integration.py` 脚本，使其能够：
1.  自动解压 `.ccx` 插件文件。
2.  尝试将解压后的插件文件夹复制到多个已知的 Photoshop 插件目录，包括用户手动尝试成功的目录。

### 行动

1.  **手动解压 `.ccx` 文件**：将 `3e6d64e0_PS.ccx` 文件重命名为 `.zip` 并解压。
2.  **发现有效安装路径**：用户通过尝试，发现将解压后的插件文件夹复制到以下三个位置之一或全部，可以使插件在 Photoshop 中显示：
    *   `/Library/Application Support/Adobe/CEP/extensions/` (系统级)
    *   `~/Library/Application Support/Adobe/CEP/extensions/` (用户级，如果 `CEP` 文件夹不存在则需手动创建)
    *   `/Applications/Adobe Photoshop 2025/Plug-ins/` (Photoshop 应用级)
3.  **更新 `setup_photoshop_integration.py` 脚本**：修改脚本，使其能够自动执行上述解压和复制操作，并检查目标目录是否存在，如果不存在则创建。

### 最终成果 (再次修订版)

*   **`setup_photoshop_integration.py`**: 更加健壮的 Python 脚本，它会自动安装 ZXP Installer（如果需要），下载所有必需的文件，并尝试将解压后的 Photoshop 插件复制到多个可能的安装位置，以提高兼容性。
*   **`Makefile`**: `install-ps-plugin` 命令将运行这个新脚本。
*   **`自动化集成操作台账.md`**: 完整记录了从最初计划到最终方案的整个决策和开发过程，并补充了手动安装插件的成功经验和自动化改进计划。


